{
  "buildCommand": "npm --prefix server run build && npm --prefix front run build",
  "outputDirectory": "front/dist",
  "installCommand": "npm --prefix server install && npm --prefix front install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
